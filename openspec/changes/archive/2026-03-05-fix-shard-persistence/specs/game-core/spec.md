<!-- No requirement changes — existing specs already define the correct behavior.
     This is a bug fix: the implementation does not match the spec.
     See game-core spec "Post-run persistence" and "Run ends in victory". -->
